# manage-services-cloudways
manage services of servers on Cloudways

* Clone the application by `git clone https://github.com/shahroznawaz/manage-services-cloudways.git`
* Run `composer install`
* Put your Cloudways **API Key** and **Email** in `index.php`
* Run the Application in browser.
